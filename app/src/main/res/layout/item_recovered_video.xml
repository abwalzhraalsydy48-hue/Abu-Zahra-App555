<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="@dimen/spacing_2xs"
    app:cardCornerRadius="@dimen/thumbnail_corner_radius"
    app:cardElevation="@dimen/elevation_none"
    app:strokeWidth="0dp"
    android:checkable="true"
    android:id="@+id/cardVideo">

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <!-- Thumbnail -->
        <ImageView
            android:id="@+id/imageThumbnail"
            android:layout_width="match_parent"
            android:layout_height="@dimen/thumbnail_grid"
            android:scaleType="centerCrop"
            android:contentDescription="@string/video_recovery_title"
            tools:background="@color/surface_variant"
            tools:src="@tools:sample/backgrounds/scenic" />

        <!-- Play Icon Center -->
        <ImageView
            android:id="@+id/imagePlayIcon"
            android:layout_width="@dimen/icon_size_huge"
            android:layout_height="@dimen/icon_size_huge"
            android:layout_gravity="center"
            android:src="@drawable/ic_play_circle"
            android:contentDescription="Play"
            app:tint="@color/text_inverse"
            android:alpha="0.8" />

        <!-- Duration Overlay -->
        <com.google.android.material.textview.MaterialTextView
            android:id="@+id/textDuration"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom|start"
            android:layout_margin="@dimen/spacing_sm"
            android:background="@drawable/bg_duration_badge"
            android:backgroundTint="@color/semi_transparent_black"
            android:paddingHorizontal="@dimen/spacing_sm"
            android:paddingVertical="@dimen/spacing_2xs"
            android:textAppearance="@style/TextAppearance.Material3.LabelSmall"
            android:textColor="@color/text_inverse"
            tools:text="03:45" />

        <!-- Checkbox Overlay -->
        <com.google.android.material.checkbox.MaterialCheckBox
            android:id="@+id/checkbox"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="top|end"
            android:layout_margin="@dimen/file_item_checkbox_margin"
            app:buttonTint="@color/primary"
            android:clickable="false"
            android:focusable="false" />

    </FrameLayout>



    <!-- Additional missing views -->
    <View android:id="@+id/viewSelectionDim" android:layout_width="match_parent" android:layout_height="match_parent" />
    <CheckBox android:id="@+id/checkboxOverlay" android:layout_width="wrap_content" android:layout_height="wrap_content" />
    <ImageView android:id="@+id/ivVideoThumbnail" android:layout_width="match_parent" android:layout_height="match_parent" android:scaleType="centerCrop" />
    <TextView android:id="@+id/tvFileName" android:layout_width="wrap_content" android:layout_height="wrap_content" />
    <TextView android:id="@+id/tvFileSize" android:layout_width="wrap_content" android:layout_height="wrap_content" />
    <TextView android:id="@+id/tvDuration" android:layout_width="wrap_content" android:layout_height="wrap_content" />

    <ImageView android:id="@+id/ivPlayIcon" android:layout_width="wrap_content" android:layout_height="wrap_content" android:src="@drawable/ic_play" />
</com.google.android.material.card.MaterialCardView>
